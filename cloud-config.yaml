# cloud-config

package_update: false
package_upgrade: false

users:
  - name: ubuntu
    groups: [sudo, admin]
    sudo: ["ALL=(ALL) NOPASSWD:ALL"]
    shell: /bin/zsh
    lock_passwd: false
    passwd: "$6$rounds=4096$Si6L7R1k0S.K7G$9XkI5Gj.9G/mXvP3YvV9rY9W.VfO1G.VfO1G.VfO1G.VfO1G.VfO1G.VfO1"

chpasswd:
  expire: false

write_files:
  - path: /tmp/setup.sh
    permissions: "0755"
    content: |
      #!/bin/bash
      exec > >(tee -a /var/log/custom_setup.log) 2>&1
      set -x

      echo "Starting setup at $(date)"

      wait_for_apt() {
        while fuser /var/lib/dpkg/lock-frontend /var/lib/apt/lists/lock /var/lib/dpkg/lock >/dev/null 2>&1; do
          sleep 5
        done
      }

      # 1. System Packages (Including fastfetch and git)
      wait_for_apt
      apt-get update
      wait_for_apt
      apt-get install -y apt-transport-https ca-certificates curl gnupg lsb-release \
        software-properties-common git unzip zsh fonts-powerline fzf tmux htop \
        build-essential jq neovim ripgrep fd-find fastfetch

      # 2. Architecture
      ARCH=$(uname -m)
      [ "$ARCH" = "x86_64" ] && { GO_ARCH="amd64"; AWS_ARCH="x86_64"; K9S_ARCH="x86_64"; } || { GO_ARCH="arm64"; AWS_ARCH="aarch64"; K9S_ARCH="arm64"; }

      # 3. Global Tools (Root)
      # Go
      curl -OL "https://golang.org/dl/go1.23.5.linux-${GO_ARCH}.tar.gz"
      tar -C /usr/local -xzf "go1.23.5.linux-${GO_ARCH}.tar.gz"
      rm "go1.23.5.linux-${GO_ARCH}.tar.gz"

      # Starship
      curl -sS https://starship.rs/install.sh | sh -s -- --yes

      # Terraform
      curl -fsSL https://apt.releases.hashicorp.com/gpg | gpg --dearmor -o /usr/share/keyrings/hashicorp-archive-keyring.gpg
      echo "deb [signed-by=/usr/share/keyrings/hashicorp-archive-keyring.gpg] https://apt.releases.hashicorp.com $(lsb_release -cs) main" > /etc/apt/sources.list.d/hashicorp.list
      wait_for_apt
      apt-get update && apt-get install -y terraform

      # Snaps
      snap install google-cloud-cli --classic
      snap install kubectl --classic

      # AWS CLI
      curl "https://awscli.amazonaws.com/awscli-exe-linux-${AWS_ARCH}.zip" -o "awscliv2.zip"
      unzip -q awscliv2.zip
      ./aws/install
      rm -rf aws awscliv2.zip

      # k9s
      curl -sL "https://github.com/derailed/k9s/releases/download/v0.27.4/k9s_Linux_${K9S_ARCH}.tar.gz" | tar xz -C /usr/local/bin/ k9s

      # 4. User Configuration (ubuntu)
      mkdir -p /home/ubuntu/.config
      cat <<EOF > /home/ubuntu/.config/starship.toml
      add_newline = true
      [character]
      success_symbol = "[➜](bold green)"
      error_symbol = "[➜](bold red)"
      EOF
      chown -R ubuntu:ubuntu /home/ubuntu/.config

      # User level script
      sudo -u ubuntu -i bash <<'U_EOF'
        # UV & NVM
        curl -LsSf https://astral.sh/uv/install.sh | sh
        curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.40.1/install.sh | bash
        
        # LazyVim
        git clone https://github.com/LazyVim/starter ~/.config/nvim
        rm -rf ~/.config/nvim/.git
        
        # ZSH Plugins
        mkdir -p ~/.zsh
        git clone https://github.com/zsh-users/zsh-autosuggestions ~/.zsh/zsh-autosuggestions
        git clone https://github.com/zsh-users/zsh-syntax-highlighting ~/.zsh/zsh-syntax-highlighting
        
        # ZSHRC Configuration
        {
          echo 'export PATH=$PATH:/usr/local/bin:/usr/local/go/bin:$HOME/go/bin:/snap/bin:$HOME/.local/bin'
          echo 'source ~/.zsh/zsh-autosuggestions/zsh-autosuggestions.zsh'
          echo 'source ~/.zsh/zsh-syntax-highlighting/zsh-syntax-highlighting.zsh'
          echo 'eval "$(starship init zsh)"'
          echo "alias vim='nvim'"
          echo 'fastfetch'
        } > ~/.zshrc
      U_EOF

      chsh -s /bin/zsh ubuntu
      echo "Finished at $(date)"

runcmd:
  - bash /tmp/setup.sh
